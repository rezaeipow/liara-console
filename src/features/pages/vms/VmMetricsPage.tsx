import { PagePlaceholder } from "../PagePlaceholder";

export default function VmMetricsPage() {
  return <PagePlaceholder title="VM Metrics" description="/console/vms/:vmId/metrics (optional)" />;
}
